const Home = () => {
    // Add search bar to search riot IDs, simple drop down with NA1, EU1, Etc...
    // Form search should load user page of person with specified name

    return (
        <div>
            <h2>Home Page</h2>
        </div>
    );
}

export default Home;